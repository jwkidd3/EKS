apiVersion: v1
kind: Service
metadata:
  name: userX-best-practices-service
  namespace: userX-namespace
  labels:
    app: best-practices-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: best-practices