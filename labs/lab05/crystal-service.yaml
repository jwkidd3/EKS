apiVersion: v1
kind: Service
metadata:
  name: userX-crystal-service
  namespace: userX-namespace
  labels:
    app: crystal-service
    owner: userX
spec:
  type: ClusterIP
  selector:
    app: crystal-backend
    owner: userX
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP