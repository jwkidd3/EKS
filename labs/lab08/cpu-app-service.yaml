apiVersion: v1
kind: Service
metadata:
  name: userX-cpu-app-service
  namespace: userX-namespace
  labels:
    app: cpu-app-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: cpu-app