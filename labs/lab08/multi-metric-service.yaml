apiVersion: v1
kind: Service
metadata:
  name: userX-multi-metric-service
  namespace: userX-namespace
  labels:
    app: multi-metric-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: multi-metric-app