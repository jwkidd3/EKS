apiVersion: v1
kind: Service
metadata:
  name: userX-database-headless
  labels:
    app: database
    owner: userX
spec:
  clusterIP: None  # This makes it a headless service
  selector:
    app: database
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres