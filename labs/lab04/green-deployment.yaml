apiVersion: apps/v1
kind: Deployment
metadata:
  name: userX-green-deployment
  namespace: userX-namespace
  labels:
    app: blue-green-app
    version: green
    owner: userX
spec:
  replicas: 3
  selector:
    matchLabels:
      app: blue-green-app
      version: green
      owner: userX
  template:
    metadata:
      labels:
        app: blue-green-app
        version: green
        owner: userX
    spec:
      containers:
      - name: nginx
        image: nginx:1.22
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
        env:
        - name: VERSION
          value: "green"
        - name: COLOR
          value: "#00FF00"